//the main for the everything where we run the game.
// author: Codie Cottrell
#include <iostream>
#include <vector>
#include "Player.h"
using namespace std;

int main(){
    string eddie = "eddie";
    Player playerOne = Player(eddie);
    playerOne.walk();
}
